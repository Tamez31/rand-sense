On the Invoices tab inside the individual client screen, each invoice card that has amount_outstanding greater than zero must show a payment section directly below the payment history table. This section must include a numeric input field labelled "Amount Received" in RandSense styling, a Fully Paid button in mint green #A8E6C1 with dark green #145A32 text that auto-populates the input with the full outstanding amount, and a Confirm Payment button in primary green #145A32 with white text. Validate that the entered amount is greater than zero and does not exceed amount_outstanding — show a red error message if invalid. On confirm, insert a record into the Supabase payments table with invoice_id, client_id, amount_paid, and payment_date as current timestamp. Update invoices table: add to amount_paid, subtract from amount_outstanding. If amount_outstanding reaches zero set status to paid and paid_date to now. If still greater than zero set status to partial. Refresh the invoice card immediately after payment without full page reload.
Also fix the invoice number format throughout the entire app. Invoice numbers must follow the format RS-00001, RS-00002 as a simple sequential number — not RS-2026-001 or any year-based format. Query the invoices table for the highest existing invoice number, parse the numeric part, increment by one, and pad to 5 digits with leading zeros. Apply this fix everywhere invoice numbers are generated.
Commit to GitHub with message: Invoicing Part 4 — Payment input section on invoice card and invoice number format fix.