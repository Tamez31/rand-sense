Please do a full code review and repair of the invoicing and payment system in Rand-Sense.html and supabase-client.js. Check and fix the following items one by one and confirm each is working correctly before moving to the next.
First, verify the Invoices tab appears correctly inside every client screen between IRP6 and Settings tabs. It must load all invoices for that client from the Supabase invoices table and display invoice number, service type, tax year, total amount, amount paid, amount outstanding, status badge and created date for each invoice.
Second, verify the payment section appears on every invoice card where amount_outstanding is greater than zero. It must show an Amount Received numeric input field, a Fully Paid button in mint #A8E6C1 that auto-fills the outstanding amount, and a Confirm Payment button in green #145A32. Validate that entered amount is greater than zero and does not exceed amount_outstanding with a red error message if invalid.
Third, verify that clicking Confirm Payment correctly inserts a record into the Supabase payments table with invoice_id, client_id, amount_paid and payment_date, then updates the invoices table by adding to amount_paid, subtracting from amount_outstanding, setting status to partial if balance remains or paid with paid_date if balance is zero. The invoice card must re-render immediately without a full page reload.
Fourth, verify the payment history table under each invoice card correctly pulls all payment records from the Supabase payments table filtered by invoice_id and displays payment date and amount for each record.
Fifth, verify all invoice numbers throughout the app follow the format RS-00001 with no year-based formats anywhere. Verify the sequential number generator queries the invoices table, finds the highest number, increments by one and pads to 5 digits.
Fix every issue found and commit to GitHub with message: Invoicing full review and repair — payment section, invoice tab, number format and Supabase sync.